CREATE TABLE IF NOT EXISTS OEC_STUDENT
(
   STUID                    NUMERIC(20)                    NOT NULL,
   STUNAME                  VARCHAR(100),
   DESCRIPTION              VARCHAR(1000),
   PRIMARY KEY (STUID)
);
