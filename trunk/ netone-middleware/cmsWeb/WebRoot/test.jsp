<%@ page contentType="text/html; charset=UTF-8"%>
<jsp:directive.page import="oe.frame.orm.OrmerEntry" />

<%
//	OrmerEntry.fetchOrmer("coredb").fetchSqlBean().executesql("create table jptran (cnkey varchar(4000),jpkey varchar(4000),jpkeymodify varchar(4000))");

//	OrmerEntry.fetchOrmer("coredb").fetchSqlBean().executesql("insert into jptransx(cnkey,jpkey,jpkeymodify)values('爱','发','2')");
/*
OrmerEntry.fetchOrmer("coredb").fetchSqlBean().executesql("insert into jptran(cnkey,jpkey,jpkeymodify)values('爱','发','2')");
OrmerEntry.fetchOrmer("coredb").fetchSqlBean().executesql("insert into jptran(cnkey,jpkey,jpkeymodify)values('爱','发','2')");
OrmerEntry.fetchOrmer("coredb").fetchSqlBean().executesql("insert into jptran(cnkey,jpkey,jpkeymodify)values('爱','发','2')");
	String[][] abc = OrmerEntry.fetchOrmer("coredb").fetchSqlBean()
	.superQueryOri("select * from jptran;");
	for (int i = 0; i < abc.length; i++) {
		for (int j = 0; j < abc[i].length; j++) {
	System.out.print(abc[i][j] + ",");
		}
		System.out.println();
	}
	
	*/

%>

